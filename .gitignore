# === Hora da Tarefa - .gitignore (v1.1 OpenRouter) ===
# Stack: Next.js + FastAPI + Postgres + Redis + MinIO + OpenRouter (Gemma 4) + Caddy + Telegram bot

# --- Segredos / env (OPENROUTER_API_KEY nunca commita) ---
.env
.env.*
!.env.example
.env.local
.env.production
*.pem
*.key
openrouter*.json
openrouter*.key
.openrouter*
secrets/
secrets.json
secrets/openrouter*

# --- Python (FastAPI, aiogram, Celery, Pillow + openai SDK OpenRouter) ---
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.env_venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
coverage/
htmlcov/
*.egg-info/
dist/
build/

# --- Node / Next.js (frontend) ---
node_modules/
frontend/node_modules/
.next/
out/
dist/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# --- TypeScript / build ---
*.tsbuildinfo
next-env.d.ts

# --- Docker / infra local ---
docker-compose.override.yml
data/
pgdata/
redis-data/
minio-data/
caddy-data/
caddy-config/
*.sqlite
*.sqlite3
*.db

# --- Uploads e imagens de tarefas (LGPD: nunca commitar) ---
# Vale p/ originais e derivadas anonimized (1600px, sem EXIF) + fixtures live com PII real
/data/images/
uploads/
*.jpg.original
*.anon.jpg
backend/uploads/
frontend/public/uploads/
backend/tests/fixtures/live/
backend/tests/fixtures/*real*
tests/fixtures/live/

# --- OpenRouter cache / VCR (podem conter key, base64 ou PII) ---
.vcr/
**/cassettes/
*.cassette.yaml
*.cassette.yml
.openrouter_cache/
openrouter_cache/

# --- Modelos IA locais (LEGADO v1.0 - removidos no MVP OpenRouter, mantidos p/ fallback offline futuro) ---
# NOTA: padrões ancorados na raiz (/) para NÃO ignorar backend/app/models/ (SQLAlchemy)
*.gguf
*.ggml
*.bin
*.onnx
*.pt
*.safetensors
/models/
/ai/models/
ollama/
!ai/models/.gitkeep

# --- Logs / temporários (nunca logar base64, statement ou OPENROUTER_API_KEY) ---
logs/
*.log
openrouter*.log
tmp/
temp/
.cache/

# --- OS / IDE ---
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo
*~

# --- Testes / coverage ---
.coverage
junit.xml
playwright-report/
test-results/
