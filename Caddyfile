# Hora da Tarefa — Caddy (TLS automático). Ajuste o domínio em prod.
# Dev local: http://localhost:8000 direto na api (sem TLS).

{
	auto_https off
}

:80 {
	# A8: teto de corpo (uploads vão por multipart de 10MB já validado na API)
	request_body {
		max_size 11MB
	}
	reverse_proxy api:8000
}
